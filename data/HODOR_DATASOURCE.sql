CREATE TABLE HODOR_DATASOURCE 
   (	"NAME" VARCHAR2(255 BYTE) NOT NULL ENABLE, 
	"PROVIDER" VARCHAR2(255 BYTE), 
	"CONNECTIONSTRING" VARCHAR2(255 BYTE), 
	"QUERY" CLOB, 
	 CONSTRAINT "HODOR_DATASOURCE_PK" PRIMARY KEY ("NAME")
) ;